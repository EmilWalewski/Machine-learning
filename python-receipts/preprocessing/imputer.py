# from sklearn.impute import SimpleImputer
# # import numpy as np
# import pandas as pd
#
# data = {'A': [1.0, 5.0, 10.0], 'B': [2.0, 6.0, 11.0], 'C': [3.0,np.nan, 12.0], 'D': [4.0, 8.0,np.nan]}
#
# df = pd.DataFrame(data)
#
# imr = SimpleImputer(missing_values=np.nan, strategy='mean')
# imr.fit(df.values)
# imputed_data = imr.transform(df.values)
# print(imputed_data)

center_word = [0 for x in range(6)]
print(center_word)